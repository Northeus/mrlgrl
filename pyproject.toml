[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mrlgrl"
version = "0.0.1"

[project.scripts]
mrlgrl = "mrlgrl.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"mrlgrl.templates" = ["*"]
